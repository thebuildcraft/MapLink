## New Features:
- It's now possible to download and convert Bluemap tiles into Xaero Chunks via the /maplink download_tiles command. Support for the other maps will follow in future updates.
- Added a config toggle and a command to turn off the "invisibility recovery" (yea idk what to call it). Just turn it off if Maplink selects the wrong dimension on a server with a proxy.